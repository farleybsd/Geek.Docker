# Build Imagens
docker-compose build
# Execultando Arquivo Docker Compose 
docker-compose up -d 
# Criando as Tabelas
docker exec -it fusion1 python manage.py migrate
# Parando Aplicacao
docker-compose down